<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>About Us</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include ('partials/navbar') %>
        <div class="about">
            <h2>About Us</h2>
        </div>
        <hr class="container-divider">
        <div class="ab-container">
            <div class="menu">
                <h5>In This Article: </h5>
                <ul>
                    <li><a href="#our-story">Our Story</a></li>
                    <li><a href="#our-mission">Our Mission</a></li>
                    <li><a href="#community">Community</a></li>
                    <li><a href="#announcements">Announcements</a></li>
                </ul>
            </div>
            <br>
            <div class="content">
                <% if (about['Our Story']) { %>
                    <h1>NutriNourish</h1>
                    <% about['Our Story'].forEach(about=> { %>
                        <h2 id="our-story" class="stop-scroll">
                            <%= about.title %>
                        </h2>
                        <p>
                            <%= about.content %>
                        </p>
                        <% }); %>
                            <% } %>

                                <% if (about['Our Mission']) { %>
                                    <% about['Our Mission'].forEach(about=> { %>
                                        <h2 id="our-mission">
                                            <%= about.title %>
                                        </h2>
                                        <p>
                                            <%= about.content %>
                                        </p>
                                        <% }); %>
                                            <% } %>

                                                <% if (about['Community']) { %>
                                                    <% about['Community'].forEach(about=> { %>
                                                        <h2 id="community">
                                                            <%= about.title %>
                                                        </h2>
                                                        <p><%- about.content %></p> <!-- 
                                ^ this tag is used such that sections is rendered as
                                raw html, allowing it to be intepreted correctly by the browser 
                                -->
                                                        <% }); %>
                                                            <% } %>

                                                                <% if (about['Announcements']) { %>
                                                                    <% about['Announcements'].forEach(about=> { %>
                                                                        <h2 id="announcements">
                                                                            <%= about.title %>
                                                                        </h2>
                                                                        <p><%- about.content %></p>
                                                                        <% }); %>
                                                                            <% } %>
            </div>


        </div>
        </div>
        <hr class="separate">
        <footer>
            <%- include('partials/footer') %>
        </footer>
</body>

</html>